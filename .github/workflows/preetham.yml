name: Hello Preetham

on:
  push:
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello to users
        run: echo "Hello Preetham"

      - name: say hello to username
        run: echo "Hello $USER"
